name: Generate RSS and Today's Page

on:
  schedule:
    # 每2小时运行一次 (UTC时间)
    - cron: '0 */2 * * *'
  workflow_dispatch:  # 允许手动触发
    inputs:
      sites:
        description: '选择要生成的网站'
        required: false
        type: choice
        options:
          - all
          - 新智元
          - 机器之心
        default: 'all'

concurrency:
  group: pages-deploy
  cancel-in-progress: false

jobs:
  generate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Restore RSS cache
        uses: actions/cache@v4
        with:
          path: data
          key: rss-cache-${{ github.run_id }}
          restore-keys: |
            rss-cache-

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/ms-playwright
            ~/Library/Caches/ms-playwright
          key: ${{ runner.os }}-playwright-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-playwright-

      - name: Install Playwright browsers
        run: |
          playwright install chromium

      - name: Generate RSS feeds (parallel)
        run: |
          if [ "${{ github.event.inputs.sites }}" = "all" ] || [ -z "${{ github.event.inputs.sites }}" ]; then
            python scripts/generate_all_rss.py --parallel
          else
            python scripts/generate_all_rss.py --site "${{ github.event.inputs.sites }}"
          fi

      - name: Generate today's pages (parallel)
        run: |
          if [ "${{ github.event.inputs.sites }}" = "all" ] || [ -z "${{ github.event.inputs.sites }}" ]; then
            python scripts/generate_all_pages.py --parallel
          else
            python scripts/generate_all_pages.py --site "${{ github.event.inputs.sites }}"
          fi

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.DEPLOY_TOKEN }}
          publish_dir: ./output
          keep_files: true